name: CI Terraform

on:
  push:
    branches: [main, develop]
    paths:
      - "infra/terraform/**"
      - ".github/workflows/ci-terraform.yml"
      - ".github/workflows/pipeline-unified.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "infra/terraform/**"
      - ".github/workflows/ci-terraform.yml"
      - ".github/workflows/pipeline-unified.yml"
  workflow_call:
  workflow_dispatch:

jobs:
  validate-terraform:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.15.2

      - name: Resolve Terraform root
        id: tfroot
        shell: bash
        run: |
          if [ -d "infra/terraform" ]; then
            echo "path=infra/terraform" >> "$GITHUB_OUTPUT"
          elif [ -d "infra/Terraform" ]; then
            echo "path=infra/Terraform" >> "$GITHUB_OUTPUT"
          else
            echo "Could not find Terraform root under infra/terraform or infra/Terraform"
            exit 1
          fi

      - name: Terraform fmt check
        run: terraform fmt -check -recursive "${{ steps.tfroot.outputs.path }}"

      - name: Terraform init+validate (bootstrap)
        shell: bash
        run: |
          cd "${{ steps.tfroot.outputs.path }}/bootstrap"
          terraform init -backend=false
          terraform validate

      - name: Terraform init+validate (dev env)
        shell: bash
        run: |
          cd "${{ steps.tfroot.outputs.path }}/envs/dev"
          terraform init -backend=false
          terraform validate
