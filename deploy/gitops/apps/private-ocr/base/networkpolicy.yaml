apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ocr-service-ingress
spec:
  podSelector:
    matchLabels:
      app: ocr-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 8001
