apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - ocr-deployment.yaml
  - ocr-service.yaml
  # - ingress.yaml  # Disabled: backend runs on EC2 Docker (CloudFront → ALB), not K3s. Kept for future migration.
  - backend-pdb.yaml
  - ocr-pdb.yaml
