---
# Base operating-system setup for all Linux nodes.
# Run:
#   ansible-playbook -i inventory/hosts.yml playbooks/packages.yml

- name: Install base packages
  hosts: linux_nodes
  become: true
  gather_facts: true

  roles:
    - base_packages

