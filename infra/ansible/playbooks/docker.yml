---
# Docker Engine and Docker Compose plugin for runtime nodes.
# Run:
#   ansible-playbook -i inventory/hosts.yml playbooks/docker.yml

- name: Install Docker runtime
  hosts: backend_nodes:data_services_nodes:monitoring_nodes:wazuh_manager
  become: true
  gather_facts: true

  roles:
    - docker_runtime

