---
# Cài và cấu hình Promtail trên tất cả nodes
# ansible-playbook -i inventory/hosts.yml playbooks/promtail.yml
#
# Để override loki_url:
#   ansible-playbook ... -e loki_url=http://10.10.0.1:3100

- name: Deploy Promtail
  hosts: k3s_cluster
  become: true

  roles:
    - role: promtail
